<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Store Show</title>
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body>
        <nav class="homeButton">
            <a href="/store"><button>Home</button></a>
        </nav>
        <h1 class="storeName"><%=store.name %></h1>
        <div class="showItem">
            <img src="<%= store.img %>" alt="img">
            <h2><%=store.description %>.</h2>
            <h2>Price: $<%= store.price %></h2>  
        
            <h2>Quantity: <%= store.qty %></h2>
            <nav class="buy">
                <a href="/store/<%= store.id %>/buy"><button>BUY</button></a>
            </nav>
            <nav class="edit">
            <a href="/store/<%=store.id %>/edit"><button>Edit</button></a>
            </nav>
        </div> 
        <nav class="delete">
            <form action="/store/<%=store.id; %>?_method=DELETE" method="POST"><input type="submit" value="DELETE" /></form>
        </nav>
    </body>
</html>